import { success, warning, errors, defaults } from "../core/index";

const useBasitToast = () => {
  return {
    success,
    warning,
    errors,
    defaults,
  };
};

export default useBasitToast;