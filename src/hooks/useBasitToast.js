import { success, warning, errors, defaults } from "../functions/index";

const useBasitToast = () => {
  return {
    success,
    warning,
    errors,
    defaults,
  };
};

export default useBasitToast;
